# OS junk
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
$RECYCLE.BIN/

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Local-only files (in case you ever add a build step)
node_modules/
dist/
.cache/

# Don't commit a Firebase config that has live keys you'd rather keep local
# (delete this line if you're fine with the keys being public — for a likes-counter
# DB scoped only to /likes with public rules, that's an acceptable trade-off)
# firebase-config.local.js
